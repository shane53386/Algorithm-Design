#include <iostream>
#include <vector>

using namespace std;

int main()
{
    int n, w, e;
    cin >> n >> w;
    vector<int> v;
    for (int i = 0; i < n; i++)
    {
        cin >> e;
        v.push_back(e);
    }
}