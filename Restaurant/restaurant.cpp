#include <iostream>
#include <vector>

using namespace std;

int order(int )

int main() {
    int N,A,c;
    vector<int> chef;
    
    cin >> N >> A;
    for (int i = 0; i < N; i++) {
        cin >> c;
        chef.push_back(c);
    }
    return 0;
}